This is a simple HTTP server for rack-based applications for learning purposes.

What it supports:
* GET HTTP Requests
* Any rack-based application

What it does not support:
* POST HTTP Requests
* Concurrent connections